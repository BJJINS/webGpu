{
  "dependencies": {
    "esbuild": "^0.21.1",
    "typescript": "^5.4.5"
  },
  "type": "module",
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=out.js",
    "start": "node main.js"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40"
  },
  "repository": "https://github.com/BJJINS/webGpu.git"
}
